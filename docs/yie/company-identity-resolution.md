# Company identity resolution

Exact canonical domain is the strongest automatic link. Exact legal name plus matching location may merge when no domain conflicts. Name similarity alone never merges: it creates or flags an ambiguous candidate for later review. Pocket 5 prefers false negatives over destructive false-positive merges.

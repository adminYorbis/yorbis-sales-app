# Discovery replay

Replay reconstructs run metadata, execution plan, attempts, mentions, checkpoint history, and aggregate artifact counts entirely from persisted records. Replay is read-only: it does not invoke providers, add checkpoints, or mutate run status.
